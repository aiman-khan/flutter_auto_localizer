name: flutter_auto_localizer
description: A Flutter package for automating localization file generation and translation.
version: 1.0.0
homepage: https://example.com/flutter_auto_localizer 
repository: https://github.com/aiman-khan/flutter_auto_localizer

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.0
  translator: ^1.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
